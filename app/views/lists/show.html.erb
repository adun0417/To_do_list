<div class="container col-md-8 col-md-offset-2">
  <table class = "table table-striped table-condensed table-bordered">
    </tr>
      <td><%= @list.name %></td>
      <td><%= @list.due_date %></td>
      <td><%= @list.note %></td>
    </tr>
  </table>
<%= link_to "<span class='glyphicon glyphicon-share-alt'>回到上一頁</span>".html_safe, lists_path, class: "btn btn-info"%>

</div>
